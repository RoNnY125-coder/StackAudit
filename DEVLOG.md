# Dev Log

## Day 1 — 2026-05-07
**Hours worked:** 7
**What I did:** Initialized Next.js project with TypeScript, Tailwind, shadcn/ui. Set up Supabase with audits and leads tables. Deployed to Vercel at https://stack-audit-two.vercel.app. Set up GitHub Actions CI — confirmed green checkmark. Researched and documented pricing for all 8 required AI tools in PRICING_DATA.md. Got Gemini and Resend API keys. Sent outreach messages to people for user interviews.
**What I learned:** Supabase free tier only allows 2 projects — reused existing project. PowerShell uses different syntax than bash for multiline commands. Next.js project names cannot have capital letters on npm.
**Blockers / what I'm stuck on:** Anthropic API requires payment in my region — switched to Gemini API for AI summary. Need to confirm interview times with people I reached out to.
**Plan for tomorrow:** Build the full spend input form with all 8 tools, plan dropdowns, seat inputs, and localStorage persistence. Create auditEngine.ts with savings calculation logic. Write 5 real Jest unit tests for the engine.

## Day 2 — 2026-05-08
**Hours worked:** 9
**What I did:** Designed full UI/UX flow in Figma covering all 4 screens — landing page, spend input form, audit results page, and email capture modal. Generated design system using Stitch AI with Terminal Noir dark theme. Agentic AI (Antigravity) built the complete component structure: landing page with Hero, Ticker, HowItWorks, SocialProof components; spend input form with ToolRow toggles, plan selectors, seat inputs, usage score slider; results page with SavingsHero animated counter, RecommendationRow breakdown, AIAnalysis block, CTABlock, ShareBar with copy and X share; SaveReportModal with email capture. Migrated from src/ directory structure to root app/ structure. Fixed Vercel deployment root directory issue. Set up Supabase tables for audits and leads. Wired real audit engine replacing mock API response. Fixed all TypeScript build errors across mock-data, RecommendationRow, and types files. Expanded audit engine to cover 9 tools with real savings logic. Audit confirmed working — shows $630/mo savings on test data with Datadog and GitHub Copilot Enterprise. ShareBar with working copy link and Share on X live on deployed URL.
**What I learned:** PowerShell Set-Content command writes itself into the file if heredoc syntax is wrong — always use VS Code to edit files directly. Vercel needs completely blank root directory when package.json is at repo root not in a subfolder. Audit engine plan name strings must exactly match TOOL_CATALOG plan strings case-sensitively or all tools return $0 savings. Next.js localStorage must be inside useEffect to avoid SSR hydration mismatch.
**Blockers / what I'm stuck on:** GitHub Copilot shows correct recommendation but $0 savings when user doesn't fill monthly spend field — need to auto-calculate spend from seats x plan price. Gemini AI summary needs GEMINI_API_KEY confirmed in Vercel env vars. Navigation links (Dashboard, Resources) go to dead routes — building tomorrow.
**Plan for tomorrow:** Build dashboard page and resources page. Fix all navigation links. Wire lead capture modal to real Supabase insert. Add localStorage persistence to form. Run Lighthouse audit and fix accessibility issues. Write all required markdown files — GTM.md, ECONOMICS.md, USER_INTERVIEWS.md, LANDING_COPY.md, METRICS.md.